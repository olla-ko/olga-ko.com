{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build:css:site && cd admin && npm ci && npm run build"
  },
  "deploy": {
    "startCommand": "node admin/server.js",
    "restartPolicyType": "ON_FAILURE"
  }
}